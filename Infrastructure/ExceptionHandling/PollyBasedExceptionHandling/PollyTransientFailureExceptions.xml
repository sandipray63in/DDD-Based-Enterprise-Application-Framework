<?xml version="1.0" encoding="utf-8" ?>
<PollyTransientFailures>
  <!--Comma seperated exceptions e.g. exceptions which might hopefully get fixed within sometime(need retry mechanism for the same) 
  or exceptions which will take sometime long to get fixed(need circuit breaker mechanism for the same)-->
  <!--policies - Comma seperated Polly policies(e.g. CircuitBreakerPolicy,RetryNTimesPolicy) to be applied for the exception -->
  <TransientFailureException assemblyName="" partialOrFullExceptionNames="">
    
  </TransientFailureException>
  
  <TransientFailureException assemblyName="" partialOrFullExceptionNames=""> 
    
  </TransientFailureException>

</PollyTransientFailures>
